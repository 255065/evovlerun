fastapi>=0.115,<0.120
uvicorn[standard]>=0.32,<0.36
pydantic>=2.10,<3
pydantic-settings>=2.7,<3
python-dotenv>=1.0
httpx>=0.27,<0.29
supabase>=2.10,<3
anthropic>=0.43,<1
cryptography>=44
python-jose[cryptography]>=3.3
# OpenAI SDK — used by the MiniMax LLM client (MiniMax exposes an
# OpenAI-compatible chat completions API at api.minimax.io).
openai>=2,<3
pytest>=8.3
pytest-asyncio>=0.25
garminconnect>=0.2.31

# MCP server. Pinned tightly: newer mcp versions (1.20+) bump starlette to
# 1.0 which is incompatible with FastAPI < 0.120 — see commit message.
mcp==1.13.1
starlette>=0.40,<0.50

