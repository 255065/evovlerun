fastapi>=0.115,<0.120
uvicorn[standard]>=0.32,<0.36
pydantic>=2.10,<3
pydantic-settings>=2.7,<3
python-dotenv>=1.0
httpx>=0.27,<0.29
supabase>=2.10,<3
cryptography>=44
python-jose[cryptography]>=3.3
pytest>=8.3
pytest-asyncio>=0.25
garminconnect>=0.2.31

# Stripe — V1 subscription billing. Stripe API version is pinned in the
# client code, not here, so library upgrades don't change wire format.
stripe>=11.0,<13

# Rate limiting for public endpoints (in-memory moving-window, per-IP).
limits>=3,<6

# MCP server. Pinned tightly: newer mcp versions (1.20+) bump starlette to
# 1.0 which is incompatible with FastAPI < 0.120 — see commit message.
mcp==1.13.1
starlette>=0.40,<0.50

